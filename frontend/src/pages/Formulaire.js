import React from 'react';
import Form from '../Components.js/Form';
const Formulaire = () => {
    return (
        <div>
            <h1> formulaire</h1>
            <Form />
        </div>
    );
};

export default Formulaire;